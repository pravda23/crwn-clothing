// in the action type string value, we use prefixes like "user/" to be explicit about which action we are defining. It may be omitted but does make the code clearer

export const USER_ACTION_TYPES = {
  SET_CURRENT_USER: "user/SET_CURRENT_USER",
};
