// we use the prefix "category/" to be explicit about which action we are defining. It may be omitted but does make the code clearer

export const CATEGORIES_ACTION_TYPES = {
  SET_CATEGORIES: "category/SET_CATEGORIES_MAP",
};
